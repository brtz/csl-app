<%= simple_form_for :user,
:url => url_for(:action => 'auth', :controller => 'authentication') do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.button :submit, 'Log In' %>
<% end %>
